[package]
name = "darling-debug-runner"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
chrono = "0.4"
clap = { version = "4.6", features = ["derive"] }
nix = { version = "0.31", features = ["process", "signal"] }
regex = "1.12"
shell-escape = "0.1"
